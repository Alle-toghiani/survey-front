export enum RoutesEnum {
  SURVEYS = 'surveys',
  SURVEY_PREVIEW = 'preview',
  SURVEY_ID_PARAM = 'sid',
  QUESTION_ID_PARAM = 'qid',
  REPORT_PARAM = 'reportCode',

  AUTH = 'auth',
  LOGIN = 'login',
  SIGNUP = 'signup',

  API_TOKEN = 'api-token',
  ADMINS = 'manage-admins',

  NOT_FOUND_PAGE = 'not-found',
  DASHBOARD = 'dashboard'
}
